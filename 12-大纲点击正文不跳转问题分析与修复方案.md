# 大纲点击正文不跳转问题分析与修复方案

日期：2026-05-19  
项目：MD Editor  
问题级别：高  
状态：待审查，尚未实施代码修改

## 1. 问题现象

右侧“大纲”面板能够正确显示文档标题层级，例如：

- H3 虚拟机设置项
- H4 登录
- H4 Nginx
- H4 用户名和密码
- H4 启动服务

但用户点击大纲中的标题项后，正文编辑区域没有滚动到对应标题位置。

从截图看，当前底部状态栏显示编辑模式为“所见即所得”，因此本次问题主要发生在 WYSIWYG / Milkdown 编辑模式下。

## 2. 排查结论

大纲点击事件本身是有接线的，问题不在 `TocPanel.vue` 的点击绑定。

当前链路是：

1. `TocPanel.vue` 根据当前 Markdown 文本解析标题，得到标题所在源码行号。
2. 用户点击大纲项。
3. `TocPanel.vue` 调用 `editorStore.requestScrollToLine(item.line)`。
4. `editorStore.targetScrollLine` 被设置为目标行号。
5. 只有 `MarkdownSource.vue` 监听 `editorStore.targetScrollLine` 并执行滚动。
6. WYSIWYG 模式下实际渲染的是 `WysiwygEditor.vue` / `MilkdownEditor.vue`，它们没有监听或消费 `targetScrollLine`。

因此，在源码模式下大纲跳转理论上可以工作；在分栏模式下，因为左侧源码编辑器存在，也可以通过源码编辑器滚动带动预览同步；但在所见即所得模式下，没有任何组件处理这个滚动请求，所以点击后正文不会跳转。

## 3. 关键代码路径

### 3.1 大纲负责发出“跳到源码行”的请求

文件：`src/components/toc/TocPanel.vue`

核心逻辑：

```ts
function scrollToHeading(item: TocItem) {
  editorStore.requestScrollToLine(item.line)
}
```

大纲解析标题时记录的是 Markdown 源码行号：

```ts
items.push({
  level: match[1].length,
  text: match[2].replace(/[#*`\[\]()]/g, '').trim(),
  line: i + 1,
})
```

这说明当前大纲跳转协议是“按源码行号跳转”。

### 3.2 editor store 只保存目标行号

文件：`src/stores/editor.ts`

核心逻辑：

```ts
const targetScrollLine = ref<number | null>(null)

function requestScrollToLine(line: number) {
  targetScrollLine.value = line
}

function clearScrollRequest() {
  targetScrollLine.value = null
}
```

这里没有携带标题文本、标题层级、标题序号、ProseMirror 坐标等信息，只有源码行号。

### 3.3 源码编辑器消费滚动请求

文件：`src/components/editor/MarkdownSource.vue`

核心逻辑：

```ts
watch(() => editorStore.targetScrollLine, (line) => {
  if (line == null || !view.value) return
  try {
    const docLine = view.value.state.doc.line(Math.min(line, view.value.state.doc.lines))
    view.value.dispatch({
      effects: EditorView.scrollIntoView(docLine.from, { y: 'center' }),
      selection: { anchor: docLine.from },
    })
    view.value.focus()
  } catch { /* invalid line */ }
  editorStore.clearScrollRequest()
})
```

这段代码只存在于 `MarkdownSource.vue`。

因此，只有 CodeMirror 源码编辑器能响应大纲跳转。

### 3.4 WYSIWYG 编辑器没有消费滚动请求

相关文件：

- `src/components/editor/WysiwygEditor.vue`
- `src/components/editor/MilkdownEditor.vue`

排查结果：

- `WysiwygEditor.vue` 没有引用 `useEditorStore`。
- `WysiwygEditor.vue` 没有监听 `targetScrollLine`。
- `MilkdownEditor.vue` 虽然引用了 `useEditorStore`，但主要用于格式命令、表格错误提示等，并没有监听 `targetScrollLine`。
- `MilkdownEditor.vue` 没有实现“源码行号 → ProseMirror 节点位置”的映射。

所以当编辑模式为 WYSIWYG 时，`editorStore.targetScrollLine` 被设置后没有任何有效消费者。

## 4. 为什么截图中的场景会失败

截图中的状态栏显示当前模式为“所见即所得”。

此时 `EditorContainer.vue` 渲染分支为：

```vue
<WysiwygEditor
  v-if="settingsStore.editorMode === 'wysiwyg'"
  :modelValue="fileStore.activeTab.content"
  @update:modelValue="onContentUpdate"
/>
```

`MarkdownSource.vue` 没有被渲染，因此源码模式中的滚动 watcher 不存在。

大纲点击后只会把 `editorStore.targetScrollLine` 设置成某个行号，但 WYSIWYG 分支没有任何组件把这个行号转成 ProseMirror 滚动行为。

这就是点击大纲后正文没有跳转的直接原因。

## 5. 次级问题

### 5.1 滚动请求没有 id，重复点击同一标题可能不会再次触发

当前请求结构只是：

```ts
targetScrollLine: number | null
```

如果某次点击在 WYSIWYG 模式下没有被消费，`targetScrollLine` 会停留在该行号。

之后再次点击同一个标题，因为值没有变化，Vue watcher 即使存在也可能不会触发。

更稳妥的设计是让滚动请求带唯一 id，例如：

```ts
scrollRequest: {
  id: number
  line: number
}
```

这样每次点击都会形成一次新的请求。

### 5.2 WYSIWYG 模式下大纲高亮也可能不准

`TocPanel.vue` 的当前高亮依据是：

```ts
editorStore.cursorLine
```

`MarkdownSource.vue` 会在 CodeMirror 选区变化时调用：

```ts
editorStore.updateCursor(line.number, pos - line.from + 1)
```

但 WYSIWYG / Milkdown 编辑器没有等价地维护 Markdown 源码行号。

因此在 WYSIWYG 模式下，大纲当前项高亮可能停留在默认值或上一次源码模式的光标行，不能可靠反映当前正文阅读位置。

### 5.3 当前大纲协议偏源码编辑器，不适合直接驱动 WYSIWYG

大纲记录的是 Markdown 源码行号，而 WYSIWYG 内部是 ProseMirror 文档树。

源码行号与 ProseMirror 节点没有天然的一一对应关系，尤其在以下内容中会变复杂：

- 表格
- 列表
- 代码块
- 引用块
- Front Matter
- 图片和 HTML-like 片段
- Milkdown 序列化后格式变化

因此不能简单在 Milkdown 里按“第 N 行”滚动；需要增加映射策略。

## 6. 推荐修复方向

### 方案 A：让大纲请求携带标题定位信息，而不只携带行号

建议把大纲项从：

```ts
{
  level: number
  text: string
  line: number
}
```

扩展为更适合跨编辑器的定位结构：

```ts
{
  id: string
  level: number
  text: string
  line: number
  headingIndex: number
}
```

点击时发出：

```ts
requestScrollToHeading({
  id,
  line,
  level,
  text,
  headingIndex,
})
```

源码模式仍按 `line` 跳转；WYSIWYG 模式可按 `headingIndex` 或 `level + text` 查找 ProseMirror heading 节点。

### 方案 B：在 WYSIWYG / Milkdown 中实现标题节点跳转

在 `MilkdownEditor.vue` 中增加对大纲滚动请求的监听。

核心思路：

1. 获取 `editorViewCtx`。
2. 遍历 ProseMirror 文档节点。
3. 找到第 N 个 heading，或找到与请求 `level/text/headingIndex` 匹配的 heading。
4. 创建 `TextSelection` 或 `NodeSelection` 到该位置。
5. 调用 `tr.scrollIntoView()`。
6. 聚焦编辑器。
7. 调用 `editorStore.clearScrollRequest()`。

伪代码：

```ts
watch(() => editorStore.scrollRequest, (request) => {
  if (!request) return

  editor.action((ctx) => {
    const view = ctx.get(editorViewCtx)
    const pos = findHeadingPos(view.state.doc, request)
    if (pos == null) return

    view.dispatch(
      view.state.tr
        .setSelection(TextSelection.create(view.state.doc, pos))
        .scrollIntoView()
    )
    view.focus()
  })

  editorStore.clearScrollRequest()
})
```

### 方案 C：在 WYSIWYG DOM 层滚动到 heading 元素

如果暂时不想做 ProseMirror 文档坐标映射，可以在 `WysiwygEditor.vue` 中查询 `.ProseMirror h1, h2, h3...` DOM 元素，并按标题序号滚动。

优点：

- 实现较快。
- 对用户可见跳转有效。

缺点：

- DOM 与 ProseMirror state 之间存在脱节风险。
- 只能滚动，不能可靠设置编辑器选区。
- 标题文本重复时需要依赖 headingIndex。

更推荐作为短期修复，不建议作为长期架构。

## 7. 推荐实施方案

建议采用“方案 A + 方案 B”：

1. 将 `editorStore.targetScrollLine` 升级为带 id 的滚动请求对象。
2. 大纲点击时同时传递 `line` 和 `headingIndex`。
3. `MarkdownSource.vue` 继续按 `line` 跳转。
4. `MilkdownEditor.vue` 按 `headingIndex` 遍历 ProseMirror heading 节点并 `scrollIntoView()`。
5. 所有消费者执行后统一清理滚动请求。

这样可以保留源码模式的稳定逻辑，同时为 WYSIWYG 提供原生的 ProseMirror 跳转能力。

## 8. 建议修改文件

### 8.1 `src/stores/editor.ts`

新增滚动请求类型：

```ts
type ScrollRequest = {
  id: number
  line: number
  headingIndex?: number
  level?: number
  text?: string
}
```

将 `targetScrollLine` 替换或并行升级为 `scrollRequest`。

### 8.2 `src/components/toc/TocPanel.vue`

构建 headings 时增加稳定的 `headingIndex`。

点击时调用新的请求方法，例如：

```ts
editorStore.requestScrollToHeading({
  line: item.line,
  headingIndex: item.headingIndex,
  level: item.level,
  text: item.text,
})
```

### 8.3 `src/components/editor/MarkdownSource.vue`

从监听 `targetScrollLine` 改为监听新的 `scrollRequest`。

仍然使用 `request.line` 定位 CodeMirror 行。

### 8.4 `src/components/editor/MilkdownEditor.vue`

新增 WYSIWYG 消费者：

- 监听新的滚动请求。
- 遍历 ProseMirror 文档中的 heading 节点。
- 按 `headingIndex` 找到目标 heading。
- 通过 `TextSelection.create` 和 `tr.scrollIntoView()` 跳转。

### 8.5 可选：`WysiwygEditor.vue`

如果选择 DOM 层短期方案，则在这里通过 `.ProseMirror` 查询 heading DOM 并调用 `scrollIntoView()`。

但长期建议放在 `MilkdownEditor.vue`，因为那里能拿到 ProseMirror view。

## 9. 验收清单

### 9.1 WYSIWYG 模式

- [ ] 打开文档并切到“所见即所得”。
- [ ] 点击右侧大纲 H1/H2/H3/H4，正文能滚动到对应标题。
- [ ] 重复点击同一个大纲项，仍能再次执行跳转。
- [ ] 标题文本重复时，点击第几个标题就跳到第几个标题。
- [ ] 点击后光标或选区定位到目标标题附近。

### 9.2 源码模式

- [ ] 点击大纲后 CodeMirror 滚动到对应 Markdown 行。
- [ ] 当前源码模式原有跳转能力不回退。

### 9.3 分栏模式

- [ ] 点击大纲后左侧源码滚动到目标行。
- [ ] 右侧预览能跟随同步滚动。

### 9.4 边界场景

- [ ] 文档包含代码块中的 `#`，大纲不会误跳代码块标题。
- [ ] 文档包含重复标题。
- [ ] 文档包含表格、列表、引用块。
- [ ] 文档很长时跳转仍然明显且稳定。
- [ ] 切换模式后大纲跳转仍然可用。

## 10. 风险与注意事项

- 不建议只用标题文本匹配，因为重复标题很常见。
- 不建议继续只传 `line`，因为 WYSIWYG 不天然理解 Markdown 源码行号。
- 如果短期使用 DOM `scrollIntoView()`，要注意 `.wysiwyg-editor` 是实际滚动容器，浏览器默认滚动行为可能受到嵌套容器影响。
- `targetScrollLine` 如果继续保持纯 number，重复点击同一标题不一定触发 watcher。
- 修改时要确保 `clearScrollRequest()` 不会被某个未激活编辑器提前清理。当前渲染分支只有一个编辑器实例，风险较低；但升级请求结构后仍应保持消费方清理。

## 11. 结论

本问题的直接原因是：大纲点击只产生了“跳到 Markdown 源码行”的请求，而当前只有 CodeMirror 源码编辑器实现了该请求的消费；WYSIWYG / Milkdown 编辑器没有对应的监听和跳转逻辑。

由于截图中的当前模式是“所见即所得”，`MarkdownSource.vue` 不在渲染树内，大纲点击后没有任何组件执行正文滚动，因此表现为“点击大纲没有反应”。

推荐将大纲滚动请求升级为跨编辑器定位协议，并在 `MilkdownEditor.vue` 中按 ProseMirror heading 节点实现 WYSIWYG 跳转。
