本版本为语义化版本 **v1.1.1**（标签 `v1.1.1`）。Windows 安装包构建号为 **2026.519.1428**（`package.json` 时间戳版本，供安装包与自动更新使用）。

### 修复

- **大纲（WYSIWYG）**：点击右侧大纲可滚动到对应标题。
- **搜索替换**：非正则「全部替换」按字面量处理（如 `$100`）。
- **文件关联**：双击 `.md` / `.mkd` 等在单实例下正确打开目标文档。

### 下载

| 文件 | 说明 |
|------|------|
| `MDEditor-Setup-2026.519.1428-win-x64.exe` | Windows x64 安装包（推荐） |
| `latest.yml` / `.blockmap` | 自动更新元数据 |

---

完整日志：[CHANGELOG.md](https://github.com/anxin233/md-editor/blob/main/CHANGELOG.md)
