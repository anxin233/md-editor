# 文件关联打开目标文档失败问题分析与修复方案

日期：2026-05-19  
项目：MD Editor  
问题级别：严重  
状态：待审查，尚未实施代码修改

## 1. 问题现象

当用户在系统文件管理器中双击 `.md` 文件，或选择“使用 MD Editor 打开”某个 Markdown 文件时，系统能够成功启动 MD Editor，但应用没有打开目标文档，而是停留在欢迎页。

从用户视角看，文件关联似乎已经生效，因为程序被拉起；但真正的目标文件路径没有进入编辑器流程，因此没有生成标签页，也没有加载文档内容。

## 2. 当前代码现状

### 2.1 打包配置已声明文件关联

`package.json` 中已经配置了 `fileAssociations`：

```json
"fileAssociations": [
  {
    "ext": "md",
    "name": "Markdown Document",
    "mimeType": "text/markdown",
    "role": "Editor"
  },
  {
    "ext": "markdown",
    "name": "Markdown Document",
    "mimeType": "text/markdown",
    "role": "Editor"
  },
  {
    "ext": "mdown",
    "name": "Markdown Document",
    "mimeType": "text/markdown",
    "role": "Editor"
  }
]
```

这说明安装包层面已经告诉操作系统：MD Editor 可以作为这些扩展名的打开程序。

但这个配置只负责“把程序启动起来”，不负责“应用内部打开目标文件”。

### 2.2 主进程没有消费系统传入的文件路径

当前 `electron/main/index.ts` 主要流程是：

- 创建 `BrowserWindow`
- 注册 IPC
- 加载 `dist/index.html`
- 处理窗口控制
- 处理 `Ctrl+Tab`

目前没有发现以下关键逻辑：

- 未读取 `process.argv`
- 未使用 `app.requestSingleInstanceLock()`
- 未监听 `second-instance`
- 未监听 macOS 的 `open-file`
- 未将外部传入的文件路径发送给渲染进程
- 未对外部传入文件调用文件访问授权

因此，当 Windows 通过启动参数传入目标 `.md` 路径时，应用没有任何代码读取它。

### 2.3 文件访问授权机制没有覆盖该入口

项目已经有较严格的文件访问授权机制，核心在：

- `electron/main/security/fileAccess.ts`
- `electron/main/ipc/dialog.ipc.ts`
- `electron/main/ipc/recent.ipc.ts`

正常通过菜单“打开文件”时，会先调用：

```ts
grantMarkdownFileAndDocDirectory(filePath)
```

然后渲染进程才能通过 `window.electronAPI.file.read(filePath)` 读取文件。

最近文件入口也会先确认 recent 记录，再授权并读取。

但“系统文件关联打开”这个入口目前没有调用授权函数。即使后续简单把路径传给前端，如果主进程没有先授权，`fileStore.openPath(filePath)` 也可能因为 `assertReadText(filePath)` 被拒绝。

## 3. 根因总结

该问题不是编辑器组件问题，也不是文件读取能力本身不可用，而是应用缺少“外部文件打开路由”。

根因分三层：

1. `fileAssociations` 只完成了系统关联注册，应用层没有处理系统传入的目标文件路径。
2. 主进程没有实现首次启动、已有实例二次打开、macOS Finder 打开文件这些入口。
3. 当前文件授权模型只覆盖了对话框打开、工作区打开、最近文件打开，没有覆盖系统外部打开文件。

## 4. 影响范围

受影响场景：

- Windows：双击 `.md` 文件启动应用。
- Windows：右键 Markdown 文件，选择“打开方式 → MD Editor”。
- Windows：应用已运行时，再双击另一个 `.md` 文件。
- macOS：Finder 中双击 Markdown 文件。
- Linux：文件管理器通过文件关联启动应用。

当前最明显的用户影响：

- 用户以为文件打开失败。
- 应用只显示欢迎页。
- 最近文件列表可能出现目标文件，但本次启动不会直接打开。
- 如果应用已运行，重复双击文件可能会启动新实例或仅聚焦空窗口，行为不稳定。

## 5. 修复目标

修复后应满足：

1. 双击 `.md` 文件后，MD Editor 启动并自动打开该文件。
2. 如果应用已经运行，再双击另一个 Markdown 文件，应聚焦现有窗口并打开该文件。
3. 外部打开的文件必须经过主进程校验和授权，不能绕过现有安全边界。
4. 渲染进程尚未加载完成时，文件路径不能丢失。
5. 同一文件重复打开时，应复用现有标签页，而不是重复创建标签。
6. 保持现有菜单打开、最近文件、工作区打开逻辑不变。

## 6. 推荐修复设计

### 6.1 主进程增加外部文件打开队列

在 `electron/main/index.ts` 中维护一个待打开文件队列：

```ts
const pendingOpenFiles: string[] = []
let rendererReady = false
```

当主进程从启动参数、`second-instance` 或 `open-file` 获得文件路径时：

1. 校验路径是否是支持的 Markdown 扩展。
2. 校验文件是否存在且是文件。
3. 调用 `grantMarkdownFileAndDocDirectory(filePath)` 授权。
4. 如果渲染进程已准备好，则立即发送 IPC。
5. 否则先放入 `pendingOpenFiles`。

### 6.2 增加支持扩展名判断

建议主进程定义统一集合：

```ts
const MARKDOWN_EXT = new Set(['.md', '.markdown', '.mdown', '.mkd'])
```

注意：当前“打开文件”对话框支持 `.mkd`，但 `package.json` 的 `fileAssociations` 没有 `.mkd`。建议本次一并补齐。

### 6.3 处理首次启动参数

Windows / Linux 下，双击文件启动应用时，文件路径通常出现在 `process.argv` 中。

主进程应在 `app.whenReady()` 前后提取启动参数中的 Markdown 文件路径。

开发环境和打包环境的 `argv` 形态不同，因此不要依赖固定下标，建议遍历全部参数并筛选存在的 Markdown 文件。

伪代码：

```ts
function collectMarkdownFilesFromArgv(argv: string[]): string[] {
  return argv.filter(isSupportedMarkdownFile)
}
```

### 6.4 处理已有实例再次打开文件

Electron 推荐使用：

```ts
const gotLock = app.requestSingleInstanceLock()
```

如果没有拿到锁，当前进程退出。

如果已有实例运行，监听：

```ts
app.on('second-instance', (_event, commandLine) => {
  // 从 commandLine 中解析 Markdown 文件
  // 聚焦主窗口
  // 打开文件
})
```

这样可以避免双击文件时启动多个 MD Editor 实例，并确保目标文件进入当前窗口。

### 6.5 处理 macOS Finder 打开文件

macOS 文件关联打开通常走：

```ts
app.on('open-file', (event, filePath) => {
  event.preventDefault()
  // 打开 filePath
})
```

这个监听应尽早注册，避免应用 ready 前收到事件时丢失路径。

### 6.6 主进程向渲染进程发送打开文件事件

建议 preload 增加只读监听接口：

```ts
onOpenExternalFile: (callback: (filePath: string) => void) => () => void
```

主进程发送：

```ts
mainWindow.webContents.send('app:open-file', filePath)
```

preload 接收后转交给渲染进程。

### 6.7 渲染进程复用现有打开流程

在 `App.vue` 或单独初始化模块中监听外部打开事件：

```ts
window.electronAPI?.onOpenExternalFile(async (filePath) => {
  const opened = await fileStore.openPath(filePath)
  if (!opened) return
  settingsStore.addRecentFile(filePath, opened.fileName)
})
```

这样可以复用现有逻辑：

- `fileStore.openPath`
- `openFileTab`
- 已打开文件切换到现有标签
- 最近文件记录
- 文件 watcher

### 6.8 增加 renderer ready 回传

为避免窗口刚创建、前端还没监听 IPC 时路径丢失，建议增加：

```ts
window.electronAPI.app.readyToOpenFiles()
```

或类似 IPC：

```ts
ipcRenderer.invoke('app:renderer-ready')
```

主进程收到后：

1. 标记 `rendererReady = true`
2. flush `pendingOpenFiles`
3. 清空队列

也可以在 `did-finish-load` 后发送，但更稳妥的是让渲染进程在 store 初始化完成、监听器注册完成后主动通知主进程。

## 7. 建议修改文件

### 7.1 `electron/main/index.ts`

新增：

- 单实例锁
- 启动参数解析
- `second-instance` 处理
- `open-file` 处理
- 外部文件校验与授权
- 待打开文件队列
- 向渲染进程派发 `app:open-file`
- 渲染进程 ready IPC

### 7.2 `electron/preload/index.ts`

新增暴露 API：

```ts
app: {
  readyToOpenFiles: () => Promise<void>
  onOpenExternalFile: (callback: (filePath: string) => void) => () => void
}
```

或直接放在顶层：

```ts
onOpenExternalFile(...)
rendererReady(...)
```

推荐放入 `app` 命名空间，后续如果继续加应用级 IPC 更清晰。

### 7.3 `src/electron.d.ts`

同步补充 `window.electronAPI` 类型。

### 7.4 `src/App.vue`

在 `onMounted` 初始化完成后：

1. 注册外部打开文件监听。
2. 调用 `readyToOpenFiles()`。
3. 收到文件路径后调用 `fileStore.openPath(filePath)`。
4. 打开成功后写入最近文件。
5. `onUnmounted` 时清理监听。

### 7.5 `package.json`

建议补齐 `.mkd`：

```json
{
  "ext": "mkd",
  "name": "Markdown Document",
  "mimeType": "text/markdown",
  "role": "Editor"
}
```

## 8. 安全要求

该修复必须遵守现有文件授权模型，不能让渲染进程随意要求主进程授权任意路径。

建议主进程对外部路径做以下检查：

- 路径必须是绝对路径或可解析为绝对路径。
- 文件必须存在。
- 必须是普通文件。
- 扩展名必须在 Markdown 白名单内。
- 授权前使用 `resolve` 规整路径。
- 授权只授予该 Markdown 文件的 `readText/writeText/watch`，以及文档目录的 `readDocumentImage/saveImage`，沿用 `grantMarkdownFileAndDocDirectory`。

不要新增类似 `file:grantPathFromRenderer` 的通用 IPC，避免把授权能力下放给不可信渲染进程。

## 9. 边界场景

需要覆盖以下情况：

1. 应用未启动，双击 `C:\Users\ASUS\Desktop\123.md`。
2. 应用已启动，双击另一个 `.md` 文件。
3. 应用已启动且窗口最小化，双击 `.md` 后窗口恢复并聚焦。
4. 双击已经打开的文件，应切换到已有标签，不重复打开。
5. 文件路径包含空格、中文、括号。
6. 文件不存在或被删除时，不应崩溃。
7. 非 Markdown 文件通过“打开方式”传入时，应忽略或提示不支持。
8. macOS Finder 打开文件。
9. Linux 文件关联启动。
10. 渲染进程尚未 ready 时传入文件，ready 后仍能打开。

## 10. 验收清单

### 10.1 功能验收

- [ ] 安装版中双击 `.md` 文件，应用启动并直接打开该文件。
- [ ] 应用已运行时双击 `.md` 文件，不创建第二个实例，现有窗口打开新标签。
- [ ] 双击同一文件时切换到原标签。
- [ ] 打开的文件出现在最近文件中。
- [ ] 打开后可以正常保存。
- [ ] 打开后可以正常粘贴图片到文档目录 `images`。
- [ ] 导出时相对图片仍可正常内联。

### 10.2 安全验收

- [ ] 外部文件打开路径会经过扩展名白名单。
- [ ] 外部文件打开路径会经过存在性和文件类型检查。
- [ ] 主进程会调用 `grantMarkdownFileAndDocDirectory`。
- [ ] 渲染进程没有获得任意路径授权能力。
- [ ] 非 Markdown 文件不会被直接授权。

### 10.3 回归验收

- [ ] 菜单“打开文件”仍正常。
- [ ] 欢迎页最近文件打开仍正常。
- [ ] 工作区文件树打开文件仍正常。
- [ ] 保存、另存为、自动保存仍正常。
- [ ] `npm run type-check` 通过。
- [ ] 打包后安装版文件关联仍存在。

## 11. 推荐实现顺序

1. 在主进程补单实例锁和外部路径解析。
2. 增加路径校验与授权函数，复用 `grantMarkdownFileAndDocDirectory`。
3. 增加主进程到渲染进程的 `app:open-file` IPC。
4. preload 暴露监听接口和 renderer ready 接口。
5. `App.vue` 注册监听并复用 `fileStore.openPath`。
6. 补齐 `.mkd` 文件关联。
7. 跑类型检查。
8. 打包安装版验证真实双击文件场景。

## 12. 风险与注意事项

- 不要只在渲染进程读取 `process.argv`。当前项目启用了 `contextIsolation` 和 `sandbox`，渲染进程不应直接接触 Node 能力。
- 不要在窗口 `did-finish-load` 前直接发送文件路径，否则可能因为前端监听尚未注册而丢失。
- 不要让渲染进程传任意路径要求主进程授权。
- `second-instance` 的 `commandLine` 在不同系统和安装方式下可能包含额外参数，应遍历筛选，不要依赖固定位置。
- 便携版和安装版行为可能不同，应以安装版文件关联为准做最终验收。

## 13. 结论

该问题的本质是“文件关联只完成了系统启动应用，没有完成应用内打开目标文件”。修复重点在 Electron 主进程，而不是 Markdown 编辑器本身。

应补齐系统外部文件打开链路：系统传入路径 → 主进程解析和校验 → 主进程授权 → 等待渲染进程 ready → IPC 通知前端 → 前端复用现有打开文件逻辑。

完成后，双击 Markdown 文件应能直接打开目标文档，并保持当前项目已有的文件访问安全边界。
